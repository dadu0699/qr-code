{
  "name": "qr-code",
  "type": "module",
  "version": "1.0.0",
  "scripts": {
    "dev": "wrangler types && astro dev",
    "start": "wrangler types && astro dev",
    "build": "wrangler types && astro check && astro build",
    "preview": "wrangler types && astro preview",
    "astro": "astro",
    "functions": "wrangler pages dev ./dist",
    "publish": "astro build && npx wrangler pages deploy dist"
  },
  "dependencies": {
    "@astrojs/cloudflare": "11.0.1",
    "@astrojs/tailwind": "5.1.0",
    "astro": "4.11.5",
    "qrcode": "1.5.3",
    "tailwindcss": "3.4.4"
  },
  "devDependencies": {
    "@types/qrcode": "1.5.5",
    "wrangler": "3.63.1"
  }
}
